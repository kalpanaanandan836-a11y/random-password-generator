# Random Password Generator

A Python script that generates random secure passwords.

## Features
- Choose how many passwords to generate
- Choose the length of each password
- Uses uppercase, lowercase, numbers and special characters

## How to run
1. Install Python
2. Run the script
3. Enter the number of passwords and length
